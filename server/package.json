{
  "name": "cubefore-assistant-server",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": { "start": "node --env-file-if-exists=.env server.mjs", "test": "node --test" }
}
